{
  "$schema": "http://json-schema.org/draft-04/schema",
  "type": "object",
  "properties": {
    "clientID": {
      "type": "string",
      "description": "Your application's Client ID, as generated by https://www.twitch.tv/kraken/oauth2/clients/new"
    },
    "requireLogin": {
      "type": "boolean",
      "description": "Whether or not the dashboard should force users to login again when their Twitch session expires"
    },
    "timeBetweenUpdates": {
      "type": "number",
      "description": "How often the Twitch API should be polled to get channel and stream info"
    },
    "required": [
      "clientID"
    ]
  }
}
