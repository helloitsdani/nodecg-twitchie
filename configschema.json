{
  "$schema": "http://json-schema.org/draft-04/schema",
  "type": "object",
  "properties": {
    "timeBetweenUpdates": {
      "type": "number",
      "description": "How often the Twitch API should be polled to get channel and stream info",
      "default": 60000
    },
    "timeBetweenRetries": {
      "type": "number",
      "description": "How long twitchie should wait before retrying after failed Twitch API lookups",
      "default": 30000
    }
  }
}
